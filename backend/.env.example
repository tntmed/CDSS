# Oracle DB — KASOM schema (ที่อยู่ของ CDSS tables)
DB_USER=KASOM
DB_PASSWORD=your_password_here
DB_DSN=localhost:1521/XEPDB1

# HIS tables เข้าผ่าน database link @pmk (hardcoded ใน decision_engine.py)
# opddiags@pmk          — diagnosis
# labresult@pmk         — lab results
# OPD_FINANCE_HEADERS@pmk — HN → finance_no mapping
# patient@pmk           — patient name (optional)
